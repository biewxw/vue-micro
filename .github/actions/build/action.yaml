name: Build
description: Runs package build

inputs:
  PACKAGE_DIR:
    description: "The package directory for the build execution."
    required: true
    default: null

runs:
  steps:
    - run: cd ${{inputs.PACKAGE_DIR}} && yarn && yarn build
      shell: bash
