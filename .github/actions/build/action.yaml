name: Build
description: Runs package build

inputs:
  PACKAGE_NAME:
    description: "The package directory for the build execution."
    required: true
    default: null

runs:
  using: "composite"
  steps:
    - run: yarn && yarn workspace ${{ inputs.PACKAGE_NAME }} run build
      shell: bash
